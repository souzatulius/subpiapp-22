
import { ActionCardItem } from '@/types/dashboard';
import { getInitialDashboardCards } from '@/hooks/dashboard/defaultCards';

// Default cards for the communication dashboard
export const defaultComunicacaoCards: ActionCardItem[] = getInitialDashboardCards('comunicacao');
